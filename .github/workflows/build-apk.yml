name: Build Release APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Grant Execute Permission for Gradlew
      run: chmod +x gradlew
    
    - name: Create google-services.json from secrets
      run: |
        echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > app/google-services.json
        echo "✅ Created google-services.json"
    
    - name: Decode Keystore
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release-keystore.jks
        echo "✅ Keystore decoded"
        ls -lh release-keystore.jks
    
    - name: 🧹 Deep Clean - Remove ALL Cached Files
      run: |
        echo "🧹 Performing deep clean..."
        ./gradlew clean || true
        echo "🗑️ Removing all cache directories..."
        rm -rf .gradle
        rm -rf app/build
        rm -rf build
        rm -rf app/.cxx
        rm -rf */build
        rm -rf ~/.gradle/caches
        rm -rf ~/.gradle/wrapper
        find . -type d -name "build" -exec rm -rf {} + 2>/dev/null || true
        echo "✅ Deep clean completed"
    
    - name: 🔨 Build Release APK (Clean Build)
      run: |
        echo "🔨 Building all ABI variants from scratch..."
        ./gradlew assembleRelease --stacktrace --no-daemon --no-build-cache --rerun-tasks
        echo "✅ Build completed!"
      env:
        KEYSTORE_FILE: ${{ github.workspace }}/release-keystore.jks
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
    
    - name: 📦 List Generated APKs
      run: |
        echo "=== APK files found ==="
        find app/build/outputs/apk/release -name "*.apk" -exec ls -lh {} \;
    
    - name: ⬆️ Upload Universal APK
      uses: actions/upload-artifact@v4
      with:
        name: app-universal-release
        path: app/build/outputs/apk/release/app-universal-release.apk
    
    - name: ⬆️ Upload ARM64 APK
      uses: actions/upload-artifact@v4
      with:
        name: app-arm64-v8a-release
        path: app/build/outputs/apk/release/app-arm64-v8a-release.apk
    
    - name: ⬆️ Upload ARMv7 APK
      uses: actions/upload-artifact@v4
      with:
        name: app-armeabi-v7a-release
        path: app/build/outputs/apk/release/app-armeabi-v7a-release.apk
    
    - name: ⬆️ Upload x86_64 APK
      uses: actions/upload-artifact@v4
      with:
        name: app-x86_64-release
        path: app/build/outputs/apk/release/app-x86_64-release.apk
    
    - name: ⬆️ Upload x86 APK
      uses: actions/upload-artifact@v4
      with:
        name: app-x86-release
        path: app/build/outputs/apk/release/app-x86-release.apk
    
    - name: ⬆️ Upload All APKs
      uses: actions/upload-artifact@v4
      with:
        name: all-release-apks
        path: app/build/outputs/apk/release/*.apk
